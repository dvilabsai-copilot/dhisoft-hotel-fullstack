import { expect,test } from '@playwright/test';
test('RainWood storefront exposes hotels, offers, gallery and booking',async({page})=>{await page.goto('/');await expect(page.getByRole('heading',{name:/memorable stays/i})).toBeVisible();await page.getByRole('link',{name:'Hotels'}).click();await expect(page.getByRole('heading',{name:/browse rainwood destinations/i})).toBeVisible();await page.getByRole('link',{name:'Offers'}).click();await expect(page.getByRole('heading',{name:/book direct and enjoy more/i})).toBeVisible();await page.getByRole('link',{name:'Gallery'}).click();await expect(page.getByRole('heading',{name:/experience rainwood/i})).toBeVisible();});
