# Accepted RainWood demo → React implementation

- Home → dynamic section renderer
- Hotels → `/hotels`
- Hotel Detail → `/hotels/:slug`
- Booking → `/booking`
- Reservation → `/admin/reservations`
- Payments → `/admin/payments`
- Reports → `/admin/reports`
- Admin → `/admin`
- AxisRooms → `/admin/axisrooms`
- Contact/Gallery/Offers → configurable homepage sections
- Website Builder → `/admin/website`
