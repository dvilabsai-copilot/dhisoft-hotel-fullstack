import { useEffect,useState } from 'react'; import api from '../api/client'; import { fallbackSite } from '../data/fallback'; import { SitePayload } from '../types';
export function useSite(){const[data,setData]=useState<SitePayload|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState('');useEffect(()=>{api.get<SitePayload>('/website/public').then(r=>setData(r.data)).catch(e=>{setError(e.message);if(String(import.meta.env.VITE_ENABLE_DEMO_FALLBACK)==='true')setData(fallbackSite)}).finally(()=>setLoading(false))},[]);return{site:data,loading,error};}
