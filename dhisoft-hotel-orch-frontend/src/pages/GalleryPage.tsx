import { useSite } from '../hooks/useSite'; export default function GalleryPage(){const{site,loading}=useSite();if(loading||!site)return <div className="page-state">Loading gallery…</div>;const images=site.properties.flatMap(p=>(Array.isArray(p.images)?p.images:[]).map((url:string)=>({url,property:p.name})));return <main className="section"><div className="container"><div className="section-head"><div><div className="kicker">Gallery</div><h2>Experience RainWood</h2></div><p>Rooms, dining, landscapes and memorable hotel experiences.</p></div><div className="gallery-page">{images.map((x,i)=><figure key={`${x.url}-${i}`}><img src={x.url} alt={`${x.property} gallery`}/><figcaption>{x.property}</figcaption></figure>)}</div></div></main>}
