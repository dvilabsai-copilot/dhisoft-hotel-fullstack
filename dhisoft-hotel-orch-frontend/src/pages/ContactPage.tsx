import { FormEvent,useState } from 'react'; import api from '../api/client'; import { useSite } from '../hooks/useSite'; export default function ContactPage(){const{site}=useSite();const[form,setForm]=useState({propertyId:'',type:'RESERVATION',name:'',email:'',phone:'',message:'',sourcePage:'/contact'});const[notice,setNotice]=useState('');async function submit(e:FormEvent){e.preventDefault();try{await api.post('/enquiries',form);setNotice('Thank you. The RainWood reservation team has received your enquiry.');setForm({...form,name:'',email:'',phone:'',message:''})}catch(err:any){setNotice(err.response?.data?.message??'Unable to submit enquiry.')}}return <main className="section"><div className="container split"><div><div className="kicker">Contact</div><h2>Plan your RainWood stay</h2><p>Send a reservation, group, wedding, corporate or support enquiry.</p><div className="panel spaced"><p><strong>Email:</strong> reservations@rainwoodhotels.com</p><p><strong>Phone:</strong> +91 90000 00000</p><p><strong>WhatsApp:</strong> Available during reservation hours</p></div></div><form className="panel" onSubmit={submit}>{notice&&<div className="notice">{notice}</div>}<label>Property<select value={form.propertyId} onChange={e=>setForm({...form,propertyId:e.target.value})}><option value="">Any RainWood property</option>{site?.properties.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label>Enquiry Type<select value={form.type} onChange={e=>setForm({...form,type:e.target.value})}>{['RESERVATION','GROUP','WEDDING_EVENT','CORPORATE','SUPPORT'].map(x=><option key={x}>{x}</option>)}</select></label><label>Name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Phone<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Message<textarea required value={form.message} onChange={e=>setForm({...form,message:e.target.value})}/></label><button className="btn full">Send Enquiry</button></form></div></main>}
