import { Link,useParams } from 'react-router-dom'; import { useSite } from '../hooks/useSite'; export default function OfferDetailPage(){const{slug}=useParams();const{site,loading}=useSite();if(loading||!site)return <div className="page-state">Loading offer…</div>;const offer=site.offers.find(o=>o.slug===slug);if(!offer)return <div className="page-state error">Offer not found.</div>;return <main className="section"><div className="container narrow"><div className="panel offer-detail"><div className="kicker">RainWood Direct Offer</div><h2>{offer.title}</h2><p>{offer.description}</p>{offer.promoCode&&<div className="notice"><strong>Promo code:</strong> {offer.promoCode}</div>}<p>Final eligibility, blackout dates, room availability and cancellation policy are checked during booking.</p><Link className="btn gold" to={`/booking?promoCode=${offer.promoCode??''}`}>Book This Offer</Link></div></div></main>}
