import { Link } from 'react-router-dom'; import { useSite } from '../hooks/useSite'; export default function OffersPage(){const{site,loading}=useSite();if(loading||!site)return <div className="page-state">Loading offers…</div>;return <main className="section"><div className="container"><div className="section-head"><div><div className="kicker">Special Offers</div><h2>Book direct and enjoy more</h2></div><p>Offer eligibility is revalidated during booking against dates, property, promo code and availability.</p></div><div className="cards">{site.offers.map(o=><article className="card" key={o.id}><div className="card-img"><img src={o.imageUrl||'https://images.unsplash.com/photo-1540541338287-41700207dee6?auto=format&fit=crop&w=1200&q=80'} alt={o.title}/></div><div className="card-body"><div className="tag-row">{o.promoCode&&<span className="tag gold">Code: {o.promoCode}</span>}<span className="tag">Save {Number(o.discountValue).toLocaleString('en-IN')}</span></div><h3>{o.title}</h3><p>{o.description}</p><Link className="btn secondary" to={`/offers/${o.slug}`}>View Offer</Link></div></article>)}</div></div></main>}
