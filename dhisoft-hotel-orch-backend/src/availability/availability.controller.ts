import { Controller, Get, Query, Req } from '@nestjs/common'; import { ApiTags } from '@nestjs/swagger'; import { Request } from 'express'; import { Public } from '../common/public.decorator'; import { tenantIdFrom } from '../common/tenant'; import { AvailabilitySearchDto } from './dto/search.dto'; import { AvailabilityService } from './availability.service'; @ApiTags('availability') @Controller('availability') export class AvailabilityController {constructor(private service:AvailabilityService){} @Public() @Get('search') search(@Req() req:Request,@Query() dto:AvailabilitySearchDto){return this.service.search(tenantIdFrom(req),dto);}}